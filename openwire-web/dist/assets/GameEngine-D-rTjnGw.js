var e=class{getGameState(){throw Error(`${this.constructor.name} must implement getGameState()`)}calculatePayout(e,t){throw Error(`${this.constructor.name} must implement calculatePayout()`)}getRules(){throw Error(`${this.constructor.name} must implement getRules()`)}calculateResults(e){throw Error(`${this.constructor.name} must implement calculateResults()`)}},t=new Map;function n(e,n){t.set(e,n)}export{n,e as t};